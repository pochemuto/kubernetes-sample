name: Deploy to aurora

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    name: Build and push Docker Image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v2
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v1 
        with:
          registry: cr.yandex
          username: oauth
          password: ${{ secrets.REGISTRY_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v2
        with:
          context: .
          push: true
          tags: |
            ${{ secrets.REGISTRY }}/simple:${{ github.sha }}
          
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    needs: build

    steps:
    - name: Checkout Repo
      uses: actions/checkout@v2

    - run: wget https://storage.yandexcloud.net/yandexcloud-yc/install.sh && chmod +x install.sh && ./install.sh -i /home/runner/yandex-cloud
    - run: echo '${{ secrets.YC_SERVICE_ACCOUNT_KEY }}' > key.json
    - run: ls -l /home/runner/yandex-cloud/bin/yc
    - run: /home/runner/yandex-cloud/bin/yc config set service-account-key key.json

    - name: Set context
      uses: azure/k8s-set-context@v1
      with:
        method: kubeconfig
        kubeconfig: '${{ secrets.KUBE_CONFIG }}'
      id: setcontext

    - run: cat $KUBECONFIG
    - uses: Azure/k8s-deploy@v1.4
      with:
        manifests: kube/simple-app.yaml
        images: '${{ secrets.REGISTRY }}/simple:${{ github.sha }}'
